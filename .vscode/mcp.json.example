{
  "// note": "VS Code MCP config. Copy this to .vscode/mcp.json in your workspace and set the absolute path. VS Code uses the top-level key \"servers\" (Claude Desktop / Cursor / Antigravity use \"mcpServers\").",
  "servers": {
    "gb-gbc-gba-test": {
      "command": "node",
      "args": ["/absolute/path/to/gb-gbc-gba-test-mcp/harness/mcp-server.mjs"]
    }
  }
}
